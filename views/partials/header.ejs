<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>NikatSeva</title>

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-success">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">NikatSeva</a>

    <div class="d-flex gap-2">

      <!-- Always visible -->
      <a href="/providers" class="btn btn-light btn-sm">Providers</a>

      <% if (admin) { %>
        <!-- Logged in Admin -->
        <a href="/admin/dashboard" class="btn btn-warning btn-sm">
          Admin Dashboard
        </a>
        <a href="/logout" class="btn btn-danger btn-sm">
          Logout
        </a>

      <% } else if (user) { %>
        <!-- Logged in User / Provider -->
        <a href="/dashboard" class="btn btn-outline-light btn-sm">
          Dashboard
        </a>
        <a href="/logout" class="btn btn-danger btn-sm">
          Logout
        </a>

      <% } else { %>
        <!-- Not logged in -->
        <a href="/login" class="btn btn-outline-light btn-sm">Login</a>
        <a href="/admin/login" class="btn btn-outline-light btn-sm">Admin</a>
        <a href="/register" class="btn btn-warning btn-sm">Register</a>
      <% } %>

    </div>
  </div>
</nav>

<div class="container mt-5">
